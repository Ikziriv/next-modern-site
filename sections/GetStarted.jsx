'use client';

import styles from '../styles';

const GetStarted = () => (
  <section className={`${styles.paddings} relative z-10`}>
    <span className='font-bold'>GetStarted</span>
  </section>
);

export default GetStarted;