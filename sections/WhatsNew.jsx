'use client';

import styles from '../styles';

const WhatsNew = () => (
  <section className={`${styles.paddings} relative z-10`}>
    <span className='font-bold'>WhatsNew</span>
  </section>
);

export default WhatsNew;